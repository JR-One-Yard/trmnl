{
    "$schema": "https://openapi.vercel.sh/vercel.json",
    "installCommand": "rm -f pnpm-lock.yaml yarn.lock && npm install --no-package-lock",
    "buildCommand": "npm run build",
    "framework": null,
    "outputDirectory": ".next",
    "crons": [
      {
        "path": "/api/bitmap/wikipedia.bmp",
        "schedule": "0 0 * * *"
      }
    ]
  }